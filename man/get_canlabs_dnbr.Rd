% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/severity.R
\name{get_canlabs_dnbr}
\alias{get_canlabs_dnbr}
\title{Load CanLaBS v2 dNBR over a scope, salvage-masked and year-filtered}
\usage{
get_canlabs_dnbr(
  canlabs_dir,
  scope_polys,
  nbac_polys = NULL,
  fire_years = NULL,
  drop_salvage = TRUE,
  files = canlabs_v2_files()
)
}
\arguments{
\item{canlabs_dir}{Directory holding the staged CanLaBS v2 GeoTIFFs.}

\item{scope_polys}{\code{sf}/\code{sfc} scope (e.g. the fire-regime polygons).}

\item{nbac_polys}{\code{sf} NBAC perimeters with a year column; required if
\code{fire_years} is used for temporal filtering.}

\item{fire_years}{Integer vector of years to keep, or \code{NULL} for all.}

\item{drop_salvage}{Logical; if \code{TRUE} (default) sets salvage-logged pixels
(mask value 1) to \code{NA}.}

\item{files}{Named character vector of layer filenames; see \code{\link[=canlabs_v2_files]{canlabs_v2_files()}}.}
}
\value{
A single-layer \code{terra} \code{SpatRaster} (\code{dNBR}) in the CanLaBS CRS.
}
\description{
Reads the staged CanLaBS v2 GeoTIFFs, crops and masks the dNBR layer to
\code{scope_polys}, drops salvage-logged pixels, and (optionally) restricts to fires
in \code{fire_years}. Returns dNBR in the CanLaBS CRS (Canada Lambert / NAD83);
reprojecting the small scope polygon into the raster CRS avoids resampling the
national mosaic.
}
\details{
Year filtering uses the caller's NBAC perimeters (a known year column) rather
than the CanLaBS fire-year raster, whose pixel encoding is not documented in the
product readme; pass the project's NBAC polygons as \code{nbac_polys}.
}
\seealso{
Other CanLaBS fire severity:
\code{\link[=canlabs_v2_files]{canlabs_v2_files()}},
\code{\link[=classify_canlabs_dnbr]{classify_canlabs_dnbr()}},
\code{\link[=compute_observed_severity_dist_canlabs]{compute_observed_severity_dist_canlabs()}},
\code{\link[=fit_canlabs_thresholds]{fit_canlabs_thresholds()}}
}
\concept{CanLaBS fire severity}
