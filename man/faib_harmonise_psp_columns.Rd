% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ground_plots_faib.R
\name{faib_harmonise_psp_columns}
\alias{faib_harmonise_psp_columns}
\title{Harmonize the PSP compilation's column names onto the non-PSP schema}
\usage{
faib_harmonise_psp_columns(df)
}
\arguments{
\item{df}{A data frame read from one FAIB table, column names upper-cased.}
}
\value{
\code{df} with any PSP-style columns renamed to their non-PSP equivalents.
}
\description{
The two FAIB compilations publish the same quantities under different names.
Without this, binding them NA-fills every PSP row's volume, composition, and
age, which silently drops the PSP plots -- the bulk of the old-forest
observations -- from the calibration.
}
\details{
\strong{Plot summaries.} The non-PSP tables use \verb{_LS} (live standing) and \verb{_DS}
(dead standing). The PSP tables split the live pool into live / ingrowth /
veteran and publish the combined total as \verb{_LIV}, with dead as \verb{_D}. \verb{_LIV}
is the counterpart of \verb{_LS}, verified against 456 overlapping plot visits:
\code{VHA_WSV_LIV}, \code{BA_HA_LIV}, \code{STEMS_HA_LIV}, and \code{SPB_CPCT_LIV} each match
their \verb{_LS} counterpart exactly. \verb{_L} alone is \emph{not} the counterpart -- it
excludes ingrowth and veterans and differs by up to 578 m3 ha^-1.

\strong{Site age.} The non-PSP table publishes plot-mean \code{AGET_TLSO} / \code{HT_TLSO} /
\code{SI_M_TLSO} / \code{AGEB_TLSO} per species. The PSP table instead publishes
per-plot values \code{AGE_TOT1..3}, \code{HTOP1..3}, \code{SI1..3}, \code{AGE_BH1..3}. The first
plot's value is taken, which reproduces the reference extract exactly on all
430 overlapping PSP plot visits (the across-plot mean and maximum do not:
72 and 272 matches respectively). Note this is plot 1, not a plot mean.
}
