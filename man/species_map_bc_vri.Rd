% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/species_maps.R
\name{species_map_bc_vri}
\alias{species_map_bc_vri}
\title{BC VRI species code -> canonical Title-case form (one-to-one)}
\format{
Named \code{character} vector of length 30 (currently). Names are
raw VRI \code{SPECIES_CD_N} codes (uppercase); values are the
Title-case canonical form.
}
\usage{
species_map_bc_vri
}
\description{
Named character vector mapping Province of BC Vegetation Resource
Inventory (VRI) \code{SPECIES_CD_N} codes to their Title-case canonical
form. The mapping is strictly one-to-one -- each raw VRI code (e.g.
\code{"HW"}, \code{"PLI"}, \code{"SXS"}) maps to its own distinct Title-case form
(\code{"Hw"}, \code{"Pli"}, \code{"Sxs"}). No species-variant lumping or
trait-group substitution is done here; that information is preserved
so consuming projects can decide how to handle each variant based on
their study area's LANDIS-II \code{species.txt}.
}
\details{
Pass this map directly to \code{\link[=CleanUpSpeciesCodeLayer]{CleanUpSpeciesCodeLayer()}} /
\code{\link[=ProcessInitialCommunitiesData]{ProcessInitialCommunitiesData()}} only if your study area has a
\code{species.txt} slot for every Title-case form here. In practice every
study area collapses some variants (e.g. \code{PLI}/\code{PLC} -> \code{Pl}; lumping
\code{Lw} / \code{Fd} into trait-group equivalents when those species are absent
from the local \code{species.txt}). Layer those decisions on top via a
named-vector merge in the consuming project, keeping the rationale
as inline comments where the decisions are made:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{## In a consuming project's target / R script:
vri_species_mapping <- c(
  landisbc::species_map_bc_vri,

  ## ---- study-area-specific lumping (overrides above) ----
  ## Western larch -> Pl: no Larix slot in species.txt; closest
  ## trait-group is the fire-adapted conifer Pinus contorta.
  LW = "Pl",

  ## Douglas-fir -> Bl: no Pseudotsuga slot in species.txt.
  FD = "Bl",

  ## ... etc, with one comment per decision so reviewers see the WHY.
)
}\if{html}{\out{</div>}}

This separation keeps biological provenance intact in the package
(you can always inspect \code{species_map_bc_vri} to see the original VRI
codes), while making study-area-specific modelling decisions
first-class artifacts in their consuming projects where they can be
reviewed alongside the rest of the study-area parameterisation.
}
\seealso{
\code{\link[=CleanUpSpeciesCodeLayer]{CleanUpSpeciesCodeLayer()}}, \code{\link[=ProcessInitialCommunitiesData]{ProcessInitialCommunitiesData()}}

Other BC VRI to LANDIS-II initial communities:
\code{\link[=CleanMapCodes]{CleanMapCodes()}},
\code{\link[=CleanUpSpeciesCodeLayer]{CleanUpSpeciesCodeLayer()}},
\code{\link[=CreateEcoRegionsMap]{CreateEcoRegionsMap()}},
\code{\link[=CreateInitialCommunitiesCSVFile]{CreateInitialCommunitiesCSVFile()}},
\code{\link[=CreateInitialCommunitiesData]{CreateInitialCommunitiesData()}},
\code{\link[=CreateInitialCommunitiesMap]{CreateInitialCommunitiesMap()}},
\code{\link[=CreateInitialCommunitiesTextFile]{CreateInitialCommunitiesTextFile()}},
\code{\link[=CreateLandisFiles]{CreateLandisFiles()}},
\code{\link[=CreateLandisGrid]{CreateLandisGrid()}},
\code{\link[=GetBECCodes]{GetBECCodes()}},
\code{\link[=GetNonVegData]{GetNonVegData()}},
\code{\link[=MapCodeDataHash]{MapCodeDataHash()}},
\code{\link[=MergeVRI1andVRI2Data]{MergeVRI1andVRI2Data()}},
\code{\link[=ProcessInitialCommunitiesData]{ProcessInitialCommunitiesData()}},
\code{\link[=WriteEcoRegionsTextFile]{WriteEcoRegionsTextFile()}}
}
\concept{BC VRI to LANDIS-II initial communities}
