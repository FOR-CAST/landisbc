% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/severity.R
\name{get_bc_burn_severity_polys}
\alias{get_bc_burn_severity_polys}
\title{Fetch BC Fire Burn Severity (Historical) polygons via bcdata}
\usage{
get_bc_burn_severity_polys(scope_polys, fire_years)
}
\arguments{
\item{scope_polys}{\code{sf} POLYGON in the project CRS (e.g. BC Albers) covering
the fire-regime ecoregion(s) the calibration targets.}

\item{fire_years}{Integer vector of fire years.}
}
\value{
An \code{sf} data frame with columns \code{FIRE_NUMBER}, \code{FIRE_YEAR},
\code{BURN_SEVERITY_RATING} (factor: Unburned / Low / Medium / High / Unknown)
and polygon geometry clipped to \code{scope_polys}.
}
\description{
Returns the Province of British Columbia's polygon-level burn-severity ratings
(one record per FIRE_NUMBER / FIRE_YEAR / BURN_SEVERITY_RATING combination)
for the requested fire years, clipped to \code{scope_polys}. Used to compute the
observed severity-class distribution for Dynamic Fire calibration via
\code{\link[=compute_observed_severity_dist]{compute_observed_severity_dist()}}.
}
\details{
Filters on \code{FIRE_YEAR \%in\% fire_years} server-side via bcdata's lazy geodata
query, so only polygons in the requested window are downloaded.

\strong{Spatial scope.} BC Fire Burn Severity is assessed only for fires with
usable pre/post-fire Landsat coverage, so its spatial coverage is much sparser
than fire perimeters. At a single study area's scale the window may yield only
a handful of assessed polygons -- too few for a representative distribution --
so \code{scope_polys} is typically the full fire-regime ecoregion(s) the study area
falls within (not the study area itself), giving a defensible regional
reference. BC severity coverage starts in 2015; earlier fire years contribute
no observed severity data.

The bcdata source is the Province of BC "Fire Burn Severity (Historical)"
layer (Forests catalogue record \verb{c58a54e5-76b7-4921-94a7-b5998484e697},
object \code{WHSE_FOREST_VEGETATION.VEG_BURN_SEVERITY_SP}).
}
\seealso{
Other BC fire severity:
\code{\link[=bc_to_landis_severity_map]{bc_to_landis_severity_map()}},
\code{\link[=compute_observed_severity_dist]{compute_observed_severity_dist()}}
}
\concept{BC fire severity}
