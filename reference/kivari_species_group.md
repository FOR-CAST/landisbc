# Map a modelled species code to its Kivari coefficient group

Map a modelled species code to its Kivari coefficient group

## Usage

``` r
kivari_species_group(species)
```

## Arguments

- species:

  Character vector of modelled species codes.

## Value

Character vector of `species_group` values, `NA` where no factor
applies.
